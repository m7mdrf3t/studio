import { VoiceLinkClient } from '@/components/features/voice-link/voice-link-client';

export default function Home() {
  return (
    <main className="flex-grow flex items-center justify-center bg-background">
      <VoiceLinkClient />
    </main>
  );
}
